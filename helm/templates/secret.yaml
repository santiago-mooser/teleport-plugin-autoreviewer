{{- if .Values.teleport.identityFile }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "teleport-autoreviewer.fullname" . }}-identity
  labels:
    {{- include "teleport-autoreviewer.labels" . | nindent 4 }}
  {{- with (include "teleport-autoreviewer.annotations" .) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
type: Opaque
data:
  identity: {{ .Values.teleport.identityFile | b64enc }}
{{- end }}
